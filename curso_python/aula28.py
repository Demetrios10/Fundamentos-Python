# Id A identidade de um elemento na memoria é dada pela função id()
# que retorna um número inteiro único para cada objeto criado na memória.
# Esse número é o endereço de memória do objeto.



V1 = 'a'
V2 = 'b'

print(id(V1))
print(id(V2))